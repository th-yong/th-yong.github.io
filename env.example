# Admin 패널 접근 패스워드
# 로컬 개발: .env.local 파일에 설정
# GitHub Actions: Repository Settings > Secrets and variables > Actions에 ADMIN_PASSWORD로 설정
ADMIN_PASSWORD=your-admin-password-here

# GitHub Personal Access Token (repo 권한 필요)
# 생성 방법: GitHub Settings > Developer settings > Personal access tokens > Tokens (classic)
# 설정 시 Admin 패널에 자동 입력됨. 비워두면 패널에서 수동 입력.
# REPO_TOKEN=your-github-personal-access-token

# GitHub 리포지토리 정보
# GITHUB_ 접두사는 예약어이므로 REPO_ 접두사 사용
# 로컬 개발: .env.local 파일에 설정
# GitHub Actions: Repository Settings > Secrets and variables > Actions에 각각 설정
REPO_OWNER=th-yong
REPO_NAME=th-yong.github.io
REPO_BRANCH=main

# Azure OpenAI 설정 (Admin 패널 AI 글 생성용)
# 설정 시 Admin 패널에서 기술명/설명만 입력해 템플릿에 맞춘 글 자동 생성 가능
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
AZURE_OPENAI_API_KEY=your-api-key
AZURE_OPENAI_DEPLOYMENT=gpt-4o
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# 참고: Next.js는 빌드 시 ADMIN_PASSWORD를 NEXT_PUBLIC_ADMIN_PASSWORD로 자동 변환합니다.
# next.config.js의 env 섹션을 참고하세요.

